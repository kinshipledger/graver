# Graver documentation fixtures

## Run `graver`

Graver is the research engine. The `graver` command opens its command-line
interface, and `graver.application` is its public Python namespace.

## FamilySearch candidate discovery

Use the API, CLI, GEDCOM, JSON, SQLite, and WikiTree names as written.

## 1. Verify the installation

### Finding TP-01: preserve Find a Grave observations

[Open the Graver repository](https://github.com/kinshipledger/graver).

![Graver evidence-flow diagram](../../docs/assets/evidence-assessment-flow.svg)

> ## Preserve quoted headings

```shell
graver --version
```

The literal identifiers `graver`, `graver.application`, and
`kinshipledger/graver` remain lowercase.
